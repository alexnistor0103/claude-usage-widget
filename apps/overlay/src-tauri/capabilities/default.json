{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Overlay window default permissions.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "window-state:default",
    "core:window:allow-start-dragging"
  ]
}
