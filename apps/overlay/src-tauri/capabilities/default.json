{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Overlay and settings window default permissions.",
  "windows": ["main", "settings"],
  "permissions": [
    "core:default",
    "window-state:default",
    "core:window:allow-start-dragging"
  ]
}
